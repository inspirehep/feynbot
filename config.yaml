gradio:
  flagging_dir: "flagged"
  allow_flagging: "auto"
  share: False
  questions: "questions.json"
llama_index:
  data_dir: "data"
  chunk_size: 512
  similarity_top_k: 3
  streaming: True
opensearch:
  text_field: "content"
  embedding_field: "embedding"
  dim: 768
