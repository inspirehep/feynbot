name: INSPIRE-AI

services:
  backend:
    build:
      dockerfile: Dockerfile
      target: dev
    ports:
      - "8000:8000"
    environment:
      - LLM_API_BASE=http://194.12.189.112:11434
    volumes:
      - ./backend:/backend/app
      - ./tests:/backend/tests
